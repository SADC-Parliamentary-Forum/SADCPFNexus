// WS0: the first-class contract detail lives here. It currently reuses the
// existing rich detail implementation (retargeted to the /contracts API via
// contractsApi). WS2 replaces this with the tabbed contract workspace.
export { default } from "@/app/(app)/procurement/contracts/[id]/page";
