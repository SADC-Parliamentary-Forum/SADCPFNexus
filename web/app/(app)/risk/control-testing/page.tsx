"use client";

import RiskPhaseStubPage from "@/components/risk/RiskPhaseStub";

export default function RiskControlTestingPage() {
  return (
    <RiskPhaseStubPage
      title="Control Testing Campaigns"
      phase="Phase 2"
      blurb="Structured control-testing campaigns are deferred. Phase 1 ships the control register, effectiveness ratings and risk linkage."
    />
  );
}
